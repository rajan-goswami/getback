<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="full_screen_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:drawable/dialog_holo_dark_frame</item>
        <item name="android:windowFullscreen">true</item>
        <!--
    <item name="android:windowIsFloating">false</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        -->
    </style>

    <style name="settings_theme" parent="@android:style/Theme.Holo">
        <!-- <item name="android:windowBackground">@drawable/background_1</item> -->
        <!-- <item name="android:windowBackground">@android:color/background_dark</item> -->
        <!-- <item name="android:windowBackground">@android:drawable/dialog_holo_dark_frame</item> -->
        <!-- <item name="android:textColor">#0A3E8C</item> -->
    </style>

    <!-- <style name="generalnotitle" parent="@android:style/Theme.Holo.Light">
        <item name="android:windowNoTitle">true</item>
    </style> -->

</resources>